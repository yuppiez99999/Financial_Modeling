# 全池模型信号组合回测（截面排序 · OOS）· 2026-09-15

> I 轮登记待办的完成：「38 标的池 × 模型信号完整组合回测」。
> 前置：`1c35511` 模型退化诊断（方向信号无信息 → 改用截面排序信号）；
> feature_cols 契约修复（本轮）；重训生成含契约的新格式 pkl。
> 命令：`python main.py portfolio-backtest --signals rank --weights all`
> 报告：`reports/portfolio/portfolio_backtest.json`

## 一、口径

- **信号**：每日取模型概率（short_term/5d）最高的前 30% 标的做多（截面
  top-quantile，绕开未校准的绝对阈值 0.5——退化诊断明确「调阈值只是掩盖」）；
- **样本外**：与训练 `_split_data` 同款合并时间切分，信号只产生于
  **OOS cutoff 2025-09-17** 之后（238 个截面日）——对模型是真正的 OOS；
- **覆盖**：33 标的有效信号（配置池 38，缺数据/信号不足者如实跳过），
  日均持仓 11 只；
- **置信度语义**：raw model proba（未校准，spread≈1pp）——置信度臂因此
  接近等权，如实呈现；
- 成本三档（T11.2）、三臂对照、PSR/DSR/PBO 审计全部同引擎复用。

## 二、结果（OOS，238 交易日）

| 加权臂 | conservative | base | aggressive |
|------|------|------|------|
| 等权 | +0.1638（Sharpe 1.716） | +0.1877（1.938） | +0.2096（2.138） |
| 1/ATR | -0.0143 | +0.0321 | +0.0757 |
| 置信度 | +0.1638（≈等权） | +0.1877（≈等权） | +0.2096（≈等权） |

最大回撤全配置在 -6%~-9%；审计 N=44：PBO 0.154。

## 三、如实判读（按 T25.4 采信标准）

1. **DSR 全部 `insufficient_evidence`**（0.01~0.41，均未过 0.5 采信线）——
   上述正读数**不得引用为「模型有正收益能力」的证据**，仅为方向性观察；
2. 等权臂读数偏好的可能来源未排除：**市场 beta**（2025-09~2026-06 窗口
   池本身涨跌）、top-30% 截面与动量的相关性——尚无基准对照臂，Sharpe 2+
   不构成技能声明；
3. 1/ATR 臂在本窗**劣于**等权（与 3 标的机械信号冒烟结论相反）——加权臂
   的优势依赖信号性质，机械冒烟结论不可外推（T22.4「并排呈现、不设默认」
   的决策被本次复验支持）；
4. 置信度臂 ≈ 等权：概率未校准、spread≈1pp，权重几乎无区分度——校准层
   （T19.4 defer 维持）是让该臂有意义的先决条件；
5. PBO 0.154：15% 的切分里 IS 冠军在 OS 掉队，中等偏低但不为零。

## 四、本轮工程交付（随本回测一并入库）

- **feature_cols 契约修复**：训练端持久化特征契约进 pkl
  （`lightgbm_model.save/load`），推理端按名对齐、缺列/数量不符 fail-close
  （`predictor._align_features_by_contract`）；旧格式退回数量对齐但显式告警。
  退化诊断第七节的遗留隐患就此关闭；tests/test_model_feature_contract.py 6 例。
- **重训**：新格式 pkl × 3 周期（38 标的真实源数据；测试准确率
  0.497/0.482/0.473，与退化诊断一致）。
- `--signals rank --rank-quantile` CLI（report_only，affects_gate=false）。

## 五、后续（需人工决策/签字的事项）

- 校准层进主推理链路 = 反转 T19.4 defer（需人工签字）：校准后概率获得
  spread，置信度加权臂与绝对阈值方向信号才有意义；
- 等权 rank 臂若要升级为「证据」：需基准对照臂（同池 buy-and-hold / 随机
  top-30%）、多窗口复验、DSR 过 0.5 采信线——属新一轮排期范围。
