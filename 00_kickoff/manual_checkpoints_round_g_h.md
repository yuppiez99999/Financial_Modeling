# 高质量项目集成轮 · 遗留人工检查点决策包与确认记录（G1~G5 + H1~H5）

> **本文件是决策材料 + 确认记录。** 条目状态为 `pending`（待人工定夺）或
> `confirmed`（**已由用户指令确认**，带签署字段）；NPC **不代签、不代选**。
> 每条给：① 要你定什么 ② 证据在哪 ③ 现状默认值 ④ 签与不签的后果 ⑤ 怎么落。
> 机器可读清单：`schedule/manual_checkpoints.json`（守卫测试钉死：
> `completed` = 代签，禁止；`confirmed` 必须带签署人与时间）。
>
> 生成日期：2026-09-12 ｜ 来源：Issue #29（高质量项目集成）· Issue #40（高质量项目集成2）
> ｜ 范围：S11~S20 排期
>
> ---
>
> ## ⚠️ 本轮确认纪要（2026-09-12，用户指令）
>
> **指令原文**：「这11项确认 并继续按排期计划开发」（Issue #40）
> **解释**：这是**确认既有决策材料**，不是新开一轮选型。因此每条按决策包给出的
> 「**现状默认值**」落定；NPC 不代选、不放宽任何已定纪律。
>
> | # | 检查点 | 确认结论 | 决策 |
> |:-:|:---|:---|:---:|
> | 1 | T15.3 门禁结构 | 维持只读（`report_only` + `freeze_structure`），**本次不解锁门禁** | `defer` |
> | 2 | T16.4 区间口径 | 不切口径：概率距离口径作生产刻度，保形区间口径 stay `report_only` | `defer` |
> | 3 | T17.4 过拟合下限 | 本次不引入该腿 | `defer` |
> | 4 | T18.4 状态分层 | 不改配置：不进门禁/风控，也不作生产特征 | `moot_by_convention` |
> | 5 | T12.3 标签主线 | 不切换：`target_{h}d` 继续作主线 | `defer` |
> | 6 | T13.4 qlib 因子 | 明确**不纳入**生产特征集 | `reject` |
> | 7 | T14.3 数据源 | 按现状定稿：akshare 优先；期货/外汇进主线；外汇池不扩充 | `keep` |
> | 8 | T11.2 成本三档 | 定稿：0.125%/0.075%/0.030% 常量，**禁止配置覆盖** | `keep` |
> | 9 | T19.4 校准层 | 暂不进主推理链路，维持只读字段暴露 | `defer` |
> | 10 | T20.1 LLM 候选 | **不采纳**（0/3 满足准入），不放宽准入 | `reject` |
> | 11 | T20.4 LLM 链路 | **整阶段取消**，代码保留不启用 | `cancel` |
>
> **本次确认的边界（重要）**：确认的是「**现状默认值**」，**不是**改配置的授权。
> - `strategy_gate` **逐字段零变更**，`affects_gate` 恒 `false`；
> - 任何需要改门禁结构 / 阈值语义 / 概率语义的动作，**须另开 PR 逐字段 diff 后再人工确认**；
> - 特别地：**T15.3 / T18.4 / T19.4 都动「概率与阈值语义」（见 §11.2），
>   本次一律维持现状**，三者无需再一并签字——因为都没有变更发生。
>
> 阶段记账：S11~S20 十个阶段随之收官为 `completed`（`plan.json`），
> 每个阶段的 `closing` 记录「自动部分已完成 + 人工检查点已确认 + `affects_gate=false`」。

---

## 〇、总览（按「是否阻塞别的决策」排序）

| # | 检查点 | 一句话问题 | 现状默认 | 阻塞谁 |
|:-:|:---|:---|:---|:---|
| 1 | **T15.3** 门禁结构签字 | 要不要把门禁从「全样本命中率」改成「置信度子集命中率 + 覆盖率下限」双指标 | `report_only`，未改 | 唯一能改变门禁的项；阻塞 10-04 里程碑结论 |
| 2 | **T16.4** 区间口径取舍 | 置信分用「保形区间粒度 {0, 0.5}」还是「概率距离连续刻度」 | `conformal.enabled=false` | 决定 S19/H4 校准层怎么接 |
| 3 | **T17.4** 过拟合概率下限 | 门禁要不要引入「过拟合概率下限」这条腿 | 未引入 | 阻塞 S19/H4 的门禁叙事 |
| 4 | **T18.4** 状态分层用途 | 市场状态用于「特征」还是「评估/风控分层」还是都不用 | `regime.enabled=false` | 决定 H3 是否值得继续投入；与 T19.4 **耦合** |
| 5 | **T19.4** 校准层主线 | 校准后的概率是否成为门禁阈值与 API 的默认语义 | `probability_calibration.enabled=false` | 与 T15.3 / T18.4 **耦合，不能分开签** |
| 6 | **T20.1** 投研调研采纳 | 3 个 LLM 候选（0/3 满足准入）的调研结论是否采纳 | 结论已交付，默认不引入 | T20.4 整阶段去留 |
| 7 | **T20.4** 投研链路保留 | LLM 只读链路保留与否（不保留则整阶段取消） | `cancel` / `pending` | H5 是否继续投入 |
| 8 | **T12.3** 标签主线 | 三重障碍法标签要不要进训练主线 | 未进（`target_{h}d` 仍是主线） | 影响所有下游读数口径 |
| 9 | **T13.4** qlib 因子 | `factor_a158_*` 要不要进生产特征集 | 未进（`enabled=false`） | —— |
| 10 | **T14.3** 回退链顺序 | `akshare` 与 `tencent` 谁优先；期货/外汇是否进训练主线 | akshare 优先；期货/外汇 `enabled=true` | 影响标的池与训练量 |
| 11 | **T11.2** 成本三档口径 | 三档（0.125%/0.075%/0.030% 单边）是否定稿 | 草案常量，禁止配置覆盖 | 影响所有净收益读数 |

> **注**：编号 1~7 是本轮（H 轮）新增；T12.3 / T13.4 / T14.3 + T11.2 是 G 轮遗留（编号 8~11）。
> **H 轮自动任务已全部交付**，故 H 轮人工检查点**全部进清单**（T16.4/T17.4/T18.4/T19.4/T20.1/T20.4）——
> 至此再无「已交付但未登记」的检查点。
> **T11.2 已标 `completed`**（口径草案已交付并被 `factor_metrics` 消费），
> 若你要改三档参数，请直接回复，我改常量并重跑证据——**不建议改**（见 §11 与 §14）。

---

## 1️⃣ T15.3 门禁结构签字（最优先）

**要你定什么**：是否把 `strategy_gate` 的判据从「全样本命中率 ≥52%」改为
「**置信度 ≥thr 子集命中率 ≥52% 且 覆盖率 ≥5%**」双指标（thr ∈ [0.2, 0.3]）。

**证据**
- 保留期（独立、未参与任何扫描）读数：阈值↑→命中率↑**单调**，三周期无一处反转；
  thr=0.2 → 52.9%~55.6%（覆盖 17%~36%）；thr=0.3 → 55.2%~58.5%（覆盖 5.6%~14.4%）；
- 20d @ 0.3 证据最强（58.48% / 14.4%）；5d @ 0.3 覆盖 5.6% 贴 5% 下限，**最脆弱**；
- 决策单五种状态已实测：无候选 → `reject`；未挑阈值/无签字 → `defer/pending`；
  **唯一通关 = 候选阈值 + 人工签字**。
- 反向证据（必须一并看）：全样本口径三周期命中率仍 <52%；S17 CPCV 收缩后
  IC 为正但 PBO 未扫；保留期水位整体高于 walk-forward 属**时段效应**，不得择优引用。

**现状默认**：`mode: report_only`、`freeze_structure: true`，`strategy_gate` 逐字段零变更。

**签 / 不签的后果**
- **不签（推荐先做多时段复验）**：门禁维持只读，10-04 里程碑以「未解锁」如实记账；
- **签**：`approve` 批准的只是**证据**——我仍需另开 PR 逐字段 diff 给你审，才能真改配置。

**怎么落**
```bash
python main.py confidence-holdout      # ① 生成保留期报告（全池）
python main.py confidence-gate         # ② 看候选区间 → defer/pending
python main.py confidence-gate --chosen-threshold 0.3 --decided-by 安然 --reason "..."
```

---

## 2️⃣ T16.4 区间口径取舍

**要你定什么**：置信分的刻度用哪种——
- **A 保形区间口径**：粒度只有 `{0, 0.5}` 两档，能表达「排除了一个方向」，表达不了**程度**；
- **B 概率距离口径（现行 `|p−0.5|×2`）**：连续可分辨，高阈值端更有分辨率
  （thr=0.5 时 命中 60.25% / IC 0.3348 / 覆盖 44.4%，区间口径 58.21% / 0.2501 / 68.3%）。

**证据**：对照判定 **`mixed`**（不构成改善结论）；覆盖率 0.8 档实测 0.7978（临界
`anti_conservative`）、0.9 档 0.9189（`on_target`）；原始概率 **ECE 0.0904** ——
「模型说的概率」不能直接当概率用（这是 S19/H4 的输入）。

**现状默认**：`model.factors.conformal.enabled=false`，区间口径不进生产链路。

**建议口径（供参考，不代做）**：**B 作生产刻度、A 作覆盖率审计与风控旁证** ——
两者不是「谁更好」，是「要不要连续刻度」。若坚持单一口径，A 需要先解决粒度问题。

---

## 3️⃣ T17.4 过拟合概率下限

**要你定什么**：门禁是否增加一条「过拟合概率下限」腿（如 PBO < 0.5 且收缩后 IC > 0）。

**证据**
- CPCV 15 条路径，purge + embargo(5) 审计**全部通过**（overlaps=0）；
- 路径 IC 均值 **0.0542**；收缩后 best_shrunk **0.0375 仍为正** → 选择偏差
  **没有吃掉全部 IC**（支持项）；
- **但 PBO 标 `available=false`**：PBO 需要「候选 × 路径」矩阵（多组候选配置
  在同一批 CPCV 路径上的得分），本轮**未扫多候选**（那属 S15 tune 范畴），
  **不冒充** —— 这正是本轮 PBO 不可用的原因；
- 统一试验预算：跨命令累计 **19 次**（horizon-scan 8 / ic 7 / conformal-interval 4），
  本次读数应按 20 次校正。

**现状默认**：未引入；`overfit-audit` 只产报告。

**结论**：**当前不构成引入依据** —— 只有「IC 收缩后为正」这一条支持项，
PBO 缺口需要先补「多候选矩阵」扫描（我可以下一轮做）。建议 `defer`。

---

## 4️⃣ T18.4 状态分层用途（本轮新增）

**要你定什么**：市场状态（`bull/range/bear`）用于
① 作为特征进模型 ② 作为评估/风控分层（如某些状态 `withheld`）③ 都不用。

**证据（单标的冒烟，只作呈现不外推）**
- 10d 分层 **`differentiated`**（命中率极差 **0.3816**）：`range` 56.50% (177 样本) /
  `bear` 18.33% (60 样本) / 全样本基线 46.84%；
- **状态特征 A/B = `degraded`**（ΔIC −0.0065、Δhit 0.0000）→ **当普通特征没有增量**；
- `bear` 段命中率 18.33% 远低于随机 —— 模型在下跌段**方向反着做**，这正是
  H3 想解释的「IC 为正但命中率卡线」的时间维度候选成因，但**单标的不够**；
- 无前视口径为 **expanding 重训**（第 t 天只用 `[0, t]` 观测）；
  `full_sample` 口径只作差异对照并明确标注有前视。

**现状默认**：`model.factors.regime.enabled=false`。

**建议方向（供参考）**：H3 的价值更可能在**②评估/风控分层**而非①特征。
若采纳②，那是 `strategy_gate` 的又一次结构变更，须另开 PR 逐字段 diff + 人工签字。

**待补**：38 标的池完整结论 `python main.py regime`（合并后跑）。

---

## 5️⃣ T19.4 校准层进主推理链路（本轮新增）

**要你定什么**：校准后的概率是否成为**主推理链路**的默认语义 ——
① 门禁阈值吃校准概率 ② API 的 `probability` 字段直接给校准值
（`calibrated_probability` 从「附加字段」升为「主字段」）。

**证据**（5d，本地缓存池）

| 口径 | Brier | ECE |
|:---:|:---:|:---:|
| 校准前 | 0.2650 | 0.1042 |
| isotonic | 0.2563 | 0.0386 |
| **platt** | **0.2498** | **0.0025** |

ECE **0.1042 → 0.0025（−97.6%）**，Brier 同步下降 → **现行概率确实系统性高估/低估**，
校准层有实质价值。**证据较强**。

**证据（§A 校准质量 · §B 决策读数，两者必须分开看）**

§A 校准质量（`reports/probability_calibration_*.json`，样本外/复验段）：

| 周期 | 校准前 ECE | platt ECE | Brier（前→后） |
|:---:|:---:|:---:|:---:|
| 5d | 0.1042 | **0.0025** | 0.2650 → 0.2498 |
| 10d | 0.1258 | 0.0463 | 0.2713 → 0.2523 |
| 20d | 0.0710 | 0.0530 | 0.2402 → 0.2394 |

> ⚠️ 负面读数一并入库：**20d isotonic 的 Brier 反而略升**（0.2402 → 0.2420）；
> 改善幅度**随周期递减**。只看 5d 那一格就是新的选择自由度。

§B 决策读数（`reports/calibration/calibration_ablation.json`）：
`python main.py calibration-ablation` 用**同一次拟合**产出
`base` / `platt` / `isotonic` 三条腿，逐指标并排（门禁点命中率 / IC / AUC /
Brier / ECE / 阈值子集命中率），**一升一降不择优、不合成分数**。

**结构性事实（为什么 §A 与 §B 可以脱节）**：校准是**单调映射**，
因此全样本口径下方向命中率与 AUC **恒等**——差异只可能出现在
**阈值子集**口径上，而那正是 T15.3 想让门禁依赖的东西。

**现状默认**：`model.calibration.enabled=false`；校准参数已固化到
`models/probability_calibration_<horizon>.json`，API 只**追加**只读字段
（`calibrated_probability` / `uncertainty` / `calibration_applied`），既有字段逐字段不变。

**确认结论（2026-09-12，见文首纪要）**：**`defer`** —— 暂不进主推理链路，维持只读字段暴露。
- 依据：校准是单调映射，全样本口径下方向命中率与 AUC 恒等，差异只在阈值子集口径；
  ECE/Brier 改善幅度随周期递减（20d isotonic 的 Brier 反而略升），§B 决策读数消融未给出「进链路」的充分依据；
- 后果：概率语义维持现状，API 差异仅体现在新增字段；
- 若日后要「签」：主链路概率语义变更**必须与 T15.3 / T18.4 一起看**（见 §11.2），且须另开 PR 逐字段 diff。

**怎么落**
```bash
python main.py calibration-ablation     # §B 决策读数消融（同一次拟合，三腿并排）
python main.py calibration              # §A 校准质量 + 固化参数
```

---

**现状默认**：`model.factors.probability_calibration.enabled=false`；
`calibrated_probability` / `uncertainty` 为**追加字段**，
`probability` / `direction` / `signal` 逐字段不变（向后兼容已由测试钉死）。

**⚠️ 耦合**：与 **T15.3**（阈值语义）**不能分开签** —— 校准层成为默认后，
同一组 `thr ∈ [0.2, 0.3]` 的物理含义会变（对应子集 ≠ 原先子集），
覆盖率与命中率都会漂移。详见 §11.2。

**现状默认即「不签」**：概率语义维持现状，改造成本为 0；代价是证据没被用上。

---

## 6️⃣ T20.1 投研调研结论采纳（本轮新增）

**要你定什么**：3 个 LLM 投研候选的**调研结论是否采纳**（是否引入其中任一个）。

**证据**（`reports/research_assist_evaluation.json`）

| 候选 | 许可证 | A 股适配 | 离线可复现 | 判定 |
|:---|:---:|:---:|:---:|:---:|
| TauricResearch/TradingAgents | Apache-2.0 | 无 | 否 | reject |
| hsliuping/TradingAgents-CN | Apache-2.0 系 | 有 | 否 | defer |
| qusong0627/QuantMind | NOASSERTION | 部分 | 否 | reject |

**结论：0/3 同时满足「A 股适配 + 离线可复现 + 许可证明确」** → 推荐**不引入**。
证据明确。详见 `00_kickoff/research_assist_conclusion.md#三`。

**现状默认**：调研结论已交付，默认不引入。

> ⚠️ 「结论已出」≠「已采纳」—— 故该任务在 `plan.json` 中**保持 `pending`**。

---

## 7️⃣ T20.4 投研链路保留（本轮新增）

**要你定什么**：LLM 只读投研链路**保留与否**（含成本与噪声风险）；不保留则**整阶段取消**。

**证据**：离线对照**结构性不可量化**（附注不参与打分，差异恒为 0）→ 记 `unverifiable` + 止损；
`build_decision` 的 blockers：① 0 候选满足准入 ② 对照不可量化 ③ 缺人工签字。

**现状默认**：`cancel` / `pending` —— 无 `decided_by` 不放行。

**签 / 不签的后果**
- **不签（推荐，即维持 cancel）**：H5 整阶段取消，只留调研结论作为知识资产；零新增依赖；
- **签「保留」**：也不是进信号路径 —— 附注永远只挂报告层（`affects_signal` 恒 false）。
  代价是维护 LLM 调用链（网络/成本/不可复现），且**无法证明增量**。
**建议（供参考，不代做）**：**维持 cancel**。排期允许整阶段取消 ——
「排期不是必须做满的清单」。若某天有候选同时满足三项准入，可按同一决策单重跑。

---

## 8️⃣ T12.3 标签主线

**要你定什么**：三重障碍法标签（`label_tb_{h}d_bin`）是否替换现行的
固定窗口标签（`target_{h}d`）成为**训练主线**。

**证据（26 标的 / 3 折，同数据同折同模型，只变标签）**
- **10 日：新 IC +0.0613 vs 旧 +0.0274，命中 52.88% vs 49.28%**（首次跨 52% 线，
  z≈+6.3）；
- **5 日：混合**（IC −0.0037、命中 +1.23pt）→ 不构成明确改善。

**现状默认**：不进主线，`data.prediction_horizons` 零变更。

**待补**：稳健性（换种子 / 换折数）与障碍参数（`k_up/k_down/vol_window`，默认常量）未扫。
若纳入，须同步更新 `leakage_checklist.md` 并重做泄漏审查。

---

## 9️⃣ T13.4 qlib 因子

**要你定什么**：`factor_a158_*`（5 个族因子）是否进生产特征集。

**证据（26 标的 / 3 折）**：5 日 −0.0024 / +0.01pt、10 日 −0.0049 / −0.09pt
（均未改善）、20 日 +0.0008 / +0.07pt（**低于噪声**）。

**现状默认**：`qlib_alpha158.enabled=false`。**结论文档已建议不纳入** ——
与 S12（标签仅 10 日有效）一致指向「瓶颈不在特征侧」。工程能力保留。

---

## 10️⃣ T14.3 回退链顺序

**要你定什么**：① 回退链顺序 ② 期货/外汇是否进训练主线 ③ 外汇池是否扩充。

**证据**：全池 **38/38 + 宏观 5/5 = 100%**（验收线 95%）；A股+ETF 26、期货主连 10、
外汇 2；现链路 `wind → akshare → tencent → simulation`（akshare 在前 = 覆盖更广 +
提供第二通道交叉验证）。

**现状默认**：akshare 在 tencent 前；`futures.enabled`/`forex.enabled` **已置 true**
→ `collect_all()` 会把 38 个标的都纳入训练。

**提醒**：若只想「先取数、暂不参与训练」，回退这两个开关即可（数据通道保留）。

---

## 11️⃣ T11.2 成本三档口径（已交付，供复核）

**三档（单边）**：保守 0.125%（佣 0.025% + 滑 0.10%）/ 基准 0.075% /
乐观 0.030%（佣 0.010% + 滑 0.02%）。

**口径**：`net = spread − 2 × 单边成本 × (turnover × days)`（保守线性上界）。

**现状**：写成常量并登记在 docstring，**禁止配置覆盖** —— 否则「换一组参数就能翻盘」
的选择自由度又回来了。**建议维持现状**（这是纪律，不是缺省）。

---

## 十一、H 轮收口附录（Issue #40 · S16~S20）

### 11.1 检查点登记补齐

原决策包只登记到 T18.4（8 条）。**H 轮自动任务现已全部交付**
（收口字段见 `schedule/plan.json` 各阶段 `closing`），因此把 **T19.4 / T20.1 / T20.4**
一并纳入，共 **11 条**，priority 重排为 **1..11 唯一连续** —— 与文首「本轮确认纪要」
的 1~11 编号一一对应（原先有重复编号）。

| 检查点 | 阶段 | 问题 | 证据 | 现状默认 | 证据强度 |
|:---:|:---:|:---|:---|:---|:---|
| **T19.4** | S19/H4 | 校准后的概率是否成为主推理链路的默认语义 | `reports/probability_calibration_5d.json` | `enabled=false`，附加字段 | **ECE 0.1042 → 0.0025（−97.6%），证据较强** |
| **T20.1** | S20/H5 | 3 个 LLM 候选的调研结论是否采纳 | `reports/research_assist_evaluation.json` | 结论已交付，默认不引入 | 0/3 满足准入，**证据明确** |
| **T20.4** | S20/H5 | LLM 只读链路保留与否（不保留则整阶段取消） | 同上 + `_decision.json` | `cancel`（已确认） | 对照结构性不可量化 → 确认取消 |

### 11.2 ⚠️ 耦合提醒：概率 / 阈值 / 状态三项**不能分开签**

| 检查点 | 动的语义 |
|:---:|:---|
| **T15.3** | 门禁**结构 + 阈值**（双指标，`thr ∈ [0.2, 0.3]`） |
| **T19.4** | **概率语义**（校准后概率是否成为默认） |
| **T18.4** | **状态与概率的关系**（状态分层是否进 `withheld`） |

这三项操作的是同一条「**概率 → 阈值 → 信号**」链：

- T15.3 已把门禁结构与阈值绑在一起；校准层一旦成为默认语义，同一组 `thr` 的**物理含义就变了**
  （原先 `thr=0.3` 对应的子集 ≠ 校准后对应的子集，覆盖率与命中率都会变）；
- T18.4 若把状态引入 `withheld`，又是对同一链路的二次切割。

**结论：建议一起看、一起签**，否则阈值语义会漂移。

> **本轮（2026-09-12）已按「现状默认值」一并确认**：T15.3 维持 `report_only`、
> T18.4 不改配置、T19.4 不进主推理链路 —— 三项**都没有发生变更**，
> 因此阈值语义与概率语义**未漂移**，无需拆分签署。

### 11.3 本轮修正的记账偏差（自查，非功能改动）

对照 Issue #40 的 S16~S20 交付做一致性核对，发现 **3 处「已交付但未登记 / 状态虚高」**：

| 对象 | 原文 | 修正后 | 说明 |
|:---|:---|:---|:---|
| **S19 / S20 阶段字段** | 缺 `round` / `auto_acceptable_completed_at` / `auto_acceptable_verified` / `closing` | **已补齐** | S16~S18 上一轮已补，S19/S20 漏了 —— 结构不对齐，无法审计「自动部分交付到哪」 |
| **T20.1** | `completed` | **`pending`** | 与 T12.3/T14.3 同型：**调研结论已交付 ≠ 已采纳**。标 completed 就是代签 |
| **清单 doc 路径** | `manual_checkpoints.json` 引用的 H4/H5 结论文档**不存在** | **已创建** | `00_kickoff/probability_calibration_conclusion.md`、`00_kickoff/research_assist_conclusion.md` —— 引用悬空等于决策材料缺失 |

**为什么必须改**：这三处若留着，`schedule/plan.json` 就会对外宣称
「H 轮自动部分已收官、检查点均已登记」，而实际上 S19/S20 无收口字段、
T20.1 被代签、两份证据文档缺失 —— 正是本项目一直在防的**假进度**与**悬空引用**。
修正只动**状态与收口字段**，模型行为、判据、门禁**逐字段未变**。

### 11.4 交付边界表（H 轮）

| 阶段 | auto_acceptable | 交付时间 | 人工检查点 |
|:---:|:---|:---|:---|
| S16 / H1 | T16.1 / T16.2 / T16.3 | 2026-09-12 | T16.4 |
| S17 / H2 | T17.1 / T17.2 / T17.3 | 2026-09-12 | T17.4 |
| S18 / H3 | T18.1 / T18.2 / T18.3 | 2026-09-12 | T18.4 |
| S19 / H4 | T19.1 / T19.2 / T19.3 | 2026-09-12 | T19.4 |
| S20 / H5 | T20.2 / T20.3 | 2026-09-12 | T20.1 / T20.4 |

全部阶段 `affects_gate=false`、`strategy_gate` **零改动**。

阶段状态：S11~S20 已随人工确认收官为 `completed`（`closing.manual_scope` 记录对应检查点）；
人工检查点本身**永不为 `completed`**，本轮由 `pending` → `confirmed`（签字，非代签）。

### 11.5 复现纪律

```bash
python -m pytest tests/test_roadmap_manual_checkpoints.py -q   # 守卫：不得代签
python -m pytest tests -q                                      # 全量回归
python main.py calibration                                     # H4 证据（T19.4）
python main.py research-assist                                 # H5 证据（T20.1/T20.4）
```

> 每次读数前先看 `reports/trials.jsonl` 的累计次数 ——
> 选择自由度会随扫描次数上升（S17 统一试验预算）。

---

### 11.6 本轮补充的两种证据形态（供你判断证据充分性）

| 命令 | 补的是什么 | 边界 |
|:---|:---|:---|
| `python main.py calibration-ablation` | T19.4 的**决策读数**（同一次拟合、三腿并排） | 不改链路、不选点；一升一降不择优 |
| `python main.py regime --holdout-evidence` | T18.4 从「单标的冒烟」→「**合成留出**」 | **非**真实 38 标的读数，报告内显式标注 `evidence_scope` |

> 两者都只为**降低决策材料的不确定性**，都不产生结论、不代签、不改门禁。

---

---
---

## 十二、纪律声明（恒定）

- 以上 **11 条**均**由用户指令确认**（见文首「本轮确认纪要」），状态 `confirmed`
  带签署人 / 时间 / 决策 / 范围 / 证据；NPC **不代签、不代选、不代改配置**；
- H 轮（S16~S20）的**代码已交付、证据已落盘**，但这**不等于**结论已被采纳 ——
  「结论已出」与「已决策」是两件事；本轮 11 项的确认对象是「**现状默认值**」，
  不是改配置的授权（见文首确认边界）；
- 守卫测试 `tests/test_roadmap_manual_checkpoints.py` 钉死：
  人工检查点**不得标 `completed`**（代签）、不得出现在 `auto_acceptable`、
  `confirmed` 必须带签署字段、清单文件必须与 `plan.json` 一致；
- 所有相关产物 `affects_gate` 恒为 `false`，`strategy_gate` 逐字段零变更；
- **确认 ≠ 改配置**：本次无任何配置文件变更；要改门禁结构 / 阈值语义 / 概率语义，
  须另开 PR 逐字段 diff 后再人工确认。

---

## 十三、上一轮（G 轮）顺带修正的记账偏差（自查，非功能改动）

对照 Issue #29 用户的 7 项清单做一致性核对时，发现 **2 个人工检查点在
`plan.json` 里被误标 `completed`**（结论已交付，但「是否采纳」这一人工动作并未发生）：

| 检查点 | 原文 | 修正后 | 说明 |
|:---|:---|:---|:---|
| **T12.3** | `completed`（`completed_at` 2026-09-11） | **`pending`** | A/B 结论确实已入库，但「是否纳入训练主线」**从未被决策**——标 completed 等于把「结论已出」等同于「已采纳」，属假进度 |
| **T14.3** | `completed`（`completed_at` 2026-09-11） | **`pending`** | 验收数字 38/38 + 5/5 = 100% 已达标入库，但「回退链顺序 / 期货外汇是否进主线」**仍待定稿** |

**为什么必须改**：这两项若留在 `completed`，`schedule/plan.json` 就会对外
宣称「已决策」，而实际没有任何人签字 —— 正是本轮纪律要防的事。
修正只动**状态字段**，验收数字与结论文档**逐字节未变**。

**2026-09-12 追加**：上述两个误标项经用户确认后，已由 `pending` → `confirmed`
（确认对象 = 当时给出的现状默认值），`completed_at` 一律不再出现。
守卫同步升级为「签字校验」：`confirmed` 条目必须有
`confirmed_by` / `confirmed_at` / `decision` / `scope` / `out_of_scope` / 证据引用，
缺一即失败 —— **没有签字依据的 `confirmed`，与没有依据的 `completed` 一样是假进度**。

新增守卫测试 `tests/test_roadmap_manual_checkpoints.py` 钉死：
人工检查点只允许 `pending` / `confirmed`、**不得标 `completed`**、
不得进 `auto_acceptable`、决策包与排期**双向一致**。

---
---

## 十四、H 轮收口附录细则（自动任务已交付 · 决策已确认）

> 本附录说明 **`auto_acceptable` 已完成到什么程度**、**哪些仍必须人工**，
> 以及**每一项的复现命令** —— 方便你逐条复核。
> 2026-09-12：11 项检查点已经用户于 Issue #40 确认落定为 `confirmed`（确认对象 = 现状默认值），
> 但**阶段 `completed` 与检查点 `confirmed` 是两回事** —— 人工检查点永远不得为 `completed`。

### 14.1 自动任务交付边界

| 阶段 | 自动任务 | 交付物 | 复现命令 | 测试 |
|:---:|:---|:---|:---|:---:|
| S16 / H1 | T16.1~T16.3 | `src/eval/conformal_probability.py` | `python main.py conformal` | 26 例 |
| S17 / H2 | T17.1~T17.3 | `src/eval/cpcv.py`、`overfit_audit.py` | `python main.py overfit-audit` | 27 例 |
| S18 / H3 | T18.1~T18.3 | `src/eval/regime.py` | `python main.py regime` | 16 例 |
| S19 / H4 | T19.1~T19.3 | `src/eval/probability_calibration.py`、`src/inference/probability_calibrator.py`、API 字段 | `python main.py calibration` | 22 例 |
| S20 / H5 | T20.2 / T20.3（+ T20.1 调研） | `src/eval/research_assist.py` | `python main.py research-assist` | 22 例 |

**全量回归**：`python -m pytest tests -q` → **1066 passed, 4 skipped**（跳过项为可选依赖 mapie / torch）。

### 14.2 阶段收口字段（新增，可审计）

`schedule/plan.json` 的 S16~S20 已补：

- `round`：`H1`~`H5`（轮次标记）；
- `auto_acceptable_completed_at`：自动任务全部交付的时间戳；
- `auto_acceptable_verified`：`true`（已过守卫测试）；
- `closing`：结构化的「自动范围 / 人工范围 / 证据路径 / 决策包 / 验证时间 / `affects_gate`」。

**注意**：阶段 `status` 的推进受人工签字约束 —— 人工检查点 `confirmed` 且阶段
带 `confirmed_by` / `confirmed_at` 之前，**不会**被标 `completed`
（`tests/test_roadmap_s16_s20.py` 钉死）。本轮 11 项确认后，S16~S20 已具备收官依据。

**2026-09-12 收官（口径统一）**：此前存在两种等价口径 —— 确认侧置 `completed`、
收口侧按「自动交付 ≠ 阶段完成」保留 `in_progress`。本轮按**「确认即收官」**统一为
`completed`，并逐阶段补齐 `completed_at`（G 轮同批补 `auto_acceptable_completed_at`）。
**收官 ≠ 改配置**：确认对象始终是「现状默认值」，`strategy_gate` 零改动、
`affects_gate` 恒 false；**人工检查点自身永不标 `completed`**（确认与代签是两回事）。

### 14.3 复现纪律（缺依赖不静默降级）

- MAPIE 未装 → `backend=native`（如实标注，不冒充保形）；
- hmmlearn 未装 → `python main.py regime` **明确报错**（不降级为规则口径）；
- 校准参数文件缺失 → 推理期**不校准、不猜**；
- 报告缺失 → `available=false` + 原因（**不猜、不外推**）。

### 14.4 签字怎么落

- 人工检查点在 `plan.json` 里 `status ∈ {pending, confirmed}`、`auto_run=false`、无 `completed_at` / `result`；
  2026-09-12 的 11 项确认即以此形式落定（`confirmed` + 签署字段，非 `completed`）；
- 门禁类（T15.3 / T18.4 / T19.4）**没有任何**快捷签字命令 ——
  须另开 PR 逐字段 diff 给你审，再由你确认；
- 决策单类（T16.4 / T17.4 / T20.4）本身不写配置，只是材料，
  **签字动作由人工在对应 PR/Issue 里确认后由我执行**。

---

## 十五、本轮新增守卫（`tests/test_roadmap_h4_h5_evidence.py`）

- `calibration-ablation`：三腿**必须共用同一拟合**（AUC 不变性硬校验）、
  逐指标**不做择优**、负面读数保留、`affects_gate` 恒 false、无前视（校准段 < 复验段）；
- 决策包与 `plan.json`：`T19.4/T20.1/T20.4` 已进清单、priority 唯一连续、
  引用文件/文档**不得悬空**、检查点状态只允许 `pending` / `confirmed`（`completed` 即代签）；阶段收官须带人工确认字段；
- `T11.2` 记账：不得再出现 `completed_at`（防回潮）。

