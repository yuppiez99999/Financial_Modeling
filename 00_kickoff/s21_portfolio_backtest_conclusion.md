# S21（I1）组合回测闭环基线 · 结论（T21.1 + T21.2 + T21.3 已交付，T21.4 待人工检查点）

> Issue #54 · 高质量项目集成 3 轮 · 2026-09-13 交付
> 引擎：`src/eval/portfolio_backtest.py`（零新依赖）｜CLI：`python main.py portfolio-backtest`
> 报告：`reports/portfolio/portfolio_backtest.json`｜守卫：`tests/test_roadmap_s21.py`（15 passed）

## 一、交付内容

| 任务 | 交付物 |
|------|--------|
| T21.1 | 池级组合回测引擎：MOC 惯例（T 日收盘决定并成交，`shift(1)` 强制无未来函数）、等权归一（`equal_active`）、杠杆禁令（总权重 > 1 fail-close）、成本逐日扣减（换手 × 单边成本）、T11.2 三档常量单源化（`factor_metrics.T112_COST_TIERS`，与 `cost_sensitivity` 同源） |
| T21.2 | 口径一致性对照：逐标的 命中率（持仓日方向正确占比，与门禁 min_hit_rate 同源）与 IC（权重 vs 次日收益 spearman）vs 单标的净收益的跨标的皮尔逊/秩相关 + 背离清单 |
| T21.3 | `python main.py portfolio-backtest`（`--symbols` / `--min-confidence` / `--cost-levels` / `--ma-window` / `--no-contrast`），落盘报告 + S17 统一试验预算登记 |

真实数据冒烟（离线，`data/raw/` 日K，4 标的：000001.SZ / 000333.SZ / 000408.SZ / 002230.SZ，
2020-01-01 ~ 2026-06-25，1569 个共同交易日，机械 MA20 长平基线信号）：

| 成本档（T11.2） | 累计净收益 | 年化 | Sharpe | 最大回撤 | 成本拖累 |
|------|------|------|------|------|------|
| conservative 0.125% | +0.3530 | — | 0.28* | -0.6523 | 0.5558 |
| base 0.075% | +0.6116 | 7.97% | 0.4153 | -0.6092 | 0.3335 |
| aggressive 0.030% | +0.9716 | 11.52% | 0.5330 | -0.5666 | 0.1334 |

*（conservative 档精确值见 JSON 报告；表内为同一信号下三档成本的分化演示。）*

## 二、口径一致性对照（T21.2 实测，base 档）

| 标的 | 命中率 | IC | 单标的净收益 | 持仓日 |
|------|------|------|------|------|
| 000001.SZ | 0.4504 | -0.0140 | -0.1354 | 686 |
| 000333.SZ | 0.4836 | -0.0010 | +0.1629 | 794 |
| 000408.SZ | 0.5050 | +0.0099 | +3.3659 | 905 |

- 跨标的秩相关：命中率 vs PnL = **1.0**，IC vs PnL = **1.0**（n=3，仅秩序参考）；
  皮尔逊 0.8411。
- **背离实例**：000333.SZ 命中率 0.484（不过半、更不过门禁 0.52）却净盈利 +0.163
  ——「命中率读数与 PnL 读数相异」被如实点名（`hit_lt_50pct_but_profit`）。
- 与 S15 结论的关系：三只标命中率**全部低于 0.52 门禁线**，却有两只净盈利 ——
  「IC 与命中率脱节」在 PnL 层的具体形态是**命中率不决定盈亏，盈亏比与成本才决定**。
  该观察支持「组合口径与命中率口径并轨评估」的讨论，但**不构成改门禁的依据**
  （门禁零改动；取舍属 T21.4 人工检查点）。

## 三、口径与边界（必须随读数一起引用）

1. **无未来函数**：引擎 `shift(1)` 强制 MOC 持仓；守卫测试用「跳涨日才建仓」
   场景钉死（当日权重吃当日涨幅 = 测试失败）；
2. **成本不可绕过**：T11.2 三档为定稿常量，命令行只能选档不能改数值；
3. **机械基线不是策略**：MA20 信号只为给引擎喂确定性输入；换信号只改输入不改引擎；
4. **fail-close**：共同交易日不足（<3）、价格缺失（停牌未补齐）、权重越界、
   未知成本档一律拒绝出数；
5. `affects_gate=false`：报告只落 `reports/portfolio/`，不触碰 `strategy_gate`。

## 四、局限与待跑（如实登记）

- 冒烟仅 4 标的 + 机械信号；**38 标的池 × 三周期模型信号**的完整组合回测待跑
  （需要先解决训练保存模型的离线批量打分与 expanding 无前视口径，属下一批自动任务）；
- IC 与命中率在 n=3 的秩相关只作演示，不作为统计证据；
- 首日建仓计一次换手成本（窗口起点视为空仓）——与「已持仓」的现实口径有差，
  已在引擎 docstring 声明；
- 年化与 Sharpe 基于 252 交易日，未做无风险利率扣减（口径与 eval_criteria 一致）。

## 五、下一步

- T21.4 人工检查点：组合口径（净值/回撤/换手/夏普 + 一致性对照）是否纳入标准评估集；
- I2（S22）三臂对照依赖本引擎（等权臂已就绪，ATR 臂复用 `compute_atr`）；
- I5（S25）将对本报告的参数扫描计入 PSR/DSR 审计。
