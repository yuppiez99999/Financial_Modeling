# 16_（TrendCast Pro）为 28 终极量化交易系统提供策略决策依据 — 对接设计方案

> 版本：v1.1 · 日期：2026-09-09 · 状态：一期已交付并验证（E2E 冒烟通过）
> 适用范围：一期（信息参考路径，只读注入）落地 + 二期路线明确
> 仓库边界：16_ 改动提交进 monorepo 根 `e:\各种PY程序`（分支 dev/devcontainer）；28 改动提交进 28 独立仓 `e:\各种PY程序\28-终极量化交易系统8.4`。

---

## 1. 背景与目标

`16_金融市场预测模型`（TrendCast Pro，LightGBM 多周期二元方向概率模型）已恢复并纳入 git 跟踪。
目标：把 16_ 的多周期（5/10/20 日）方向预测，以 REST 方式接入 `28-终极量化交易系统8.4`（下称 28）的每日决策链路，
形成**可验证、可审计、可进入报告与 LLM 决策上下文**的信号来源，为 28 提供"策略决策依据"。

**核心原则：16_ 只出方向与概率，28 独享决策权。**
- 16_ 不输出仓位/下单建议（仓位决策权留在 28）。
- 信号一期仅作**只读上下文**注入；不改变 28 任何现有下单/调仓行为。
- 观测路径 fail-open（16_ 不可用 → 28 主流程不受影响）；决策路径 fail-close（二期打分注入前须过命中率门禁）。

---

## 2. 现状与差距（已核实）

### 16_ 侧（`16_金融市场预测模型/`）
- API（`src/api/server.py`，FastAPI，端口 8800）现有路由：`/health`、`/api/v1/models`、`/api/v1/predict/{symbol}`、`/api/v1/predict/batch`、`/api/v1/notify`、`/api/v1/audit/report`、`/api/v1/audit/stats`、`/api/v1/config/markets`。
- **缺失**：旧 28 `daily_runner` 步骤 2.5 期望的 `get_portfolio_summary()`（predictions + sector_signals 聚合端点）→ 需新增 `/api/v1/portfolio/summary`。
- 预测输出真实结构（每周期 dict，`predict_all_horizons(symbol)` 返回 `{symbol, predictions:{short_term,mid_term,long_term}}`）：
  ```json
  {
    "symbol": "600519.SH",
    "horizon": "short_term",
    "horizon_days": 5,
    "prediction": 1,
    "direction": "看涨",
    "probability": 0.62,
    "confidence": 0.62,
    "latest_date": "2026-06-27",
    "latest_close": 1500.0
  }
  ```
- 方向词汇固定为 `"看涨"` / `"看跌"`（prediction==1 → 看涨）。
- 行情缓存：`data/raw/<symbol>.csv`；`configs/config.yaml` 当前 `data.source: ["simulation"]`，无真实 CSV 时模型用**模拟数据兜底**（v1 链路可跑通，但预测精度非真实 → 属二期重训范围）。
- 历史评估结论（2026-06-27）：5d≈随机，10d/20d 较强；因此 16_ 侧审计基于模拟数据不可信——**命中率判据必须由 28 侧用真实行情计算**。

### 28 侧（`28-终极量化交易系统8.4/`）
- `daily_runner.py` 步骤 2.5 `step_trendcast_predict()`（约 273–357 行）引用 `trendcast_audit.TrendCastAudit` 与 `trendcast_client.TrendCastClient` —— 这两个模块**当前已不存在**（全仓搜索仅 `daily_runner` 与本文件引用残留）→ 步骤 2.5 现被 `ImportError` 短路为"模块未安装"。
- 步骤 2.5 调用契约（需兼容）：
  - `client.health_check()` → `{"status": "ok", ...}` 或含 `"error"`。
  - `client.get_portfolio_summary()` → `{"predictions": [...], "sector_signals": {...}}`（v1 不强依赖 sector_signals，可空）。
  - `audit.record_prediction(symbol, horizon, direction, probability, source="trendcast_pro")`。
  - `audit.verify_predictions()`、`audit.get_stats()`（返回含 `verified`、`hit_rate`）。
  - 命中率 < 50% 且 `verified > 10` → 输出漂移告警。
- 持仓来源：`config/positions.json`（gitignored），`data["positions"]` 为 dict（code→info）或 list，含 `code/name/sector` 字段。
- HTTP 客户端惯例：全仓 92+ 处使用 `requests` 库（无新依赖引入）。
- 报告注入锚点：`generate_daily_report.py` 的 `generate_report()`(返回 dict) 与 `generate_markdown_report(report)`(返回 markdown)；`daily_runner` 步骤返回值本就进入每日报告章节 → 作为信号卡片文本注入最自然、零侵入。
- 真实行情回溯：28 无统一历史行情函数（`utils/data_provider.py` 仅有 `get_price_prediction` 预测接口，无通用 OHLC 历史）→ 审计回溯采用**可插拔 `price_source`，默认 best-effort**（见 §4.3）。

---

## 3. 系统架构与数据流

```
16_ (monorepo 根 e:\各种PY程序)                28 (e:\各种PY程序\28-终极量化交易系统8.4)
─────────────────────────────                ────────────────────────────────────────
src/api/server.py  FastAPI:8800               daily_runner.py 步骤2.5
  ├─ /health                                      │
  ├─ /api/v1/portfolio/summary  [NEW]            trendcast_client.py  [NEW]
  └─ (复用 PredictionEngine + audit)             │  GET /health, GET /api/v1/portfolio/summary
        ▲                                        │
        │ HTTPS                                  ▼
        └────────────────────────────────────────┘
                                                 trendcast_audit.py  [NEW]
                                                   ├─ record_prediction → JSONL (logs/trendcast/audit/)
                                                   ├─ verify_predictions → 用 price_source 真实行情算命中
                                                   └─ get_stats → {verified, hits, hit_rate}
                                                 ▼
                                  报告/LLM 决策上下文（只读注入，fail-open）
                                    ├─ 步骤2.5 返回信号卡片文本 → 每日报告章节
                                    └─ logs/trendcast/signals_YYYY-MM-DD.json 快照（供下游消费）
```

**数据流（每日）**：
1. 16_ server 常驻（`:8800`）。
2. 28 `daily_runner` 步骤 2.5：`client.health_check()` → `client.get_portfolio_summary(symbols=28持仓)`。
3. 客户端把响应归一化为固定契约，逐条 `audit.record_prediction(...)` 落盘 JSONL；同时写 `logs/trendcast/signals_YYYY-MM-DD.json` 快照。
4. `audit.verify_predictions()` 用可插拔 `price_source`（28 真实行情）回溯已到期预测，计算命中；`get_stats()` 统计命中率，< 50% 出漂移告警。
5. 步骤 2.5 返回信号卡片文本（含命中率/漂移）→ 进入每日报告章节；快照 JSON 供 LLM 决策上下文按需读取（v1 不强制改写决策逻辑）。

---

## 4. 接口契约（跨仓共同依赖）

### 4.1 16_ 新增端点 `GET /api/v1/portfolio/summary`
- 请求：`?symbols=600519.SH,000858.SZ`（可选；缺省则用 16_ `config.data.markets` 启用标的）。
- 响应（固定契约，由 16_ 服务端归一化）：
  ```json
  {
    "generated_at": "2026-09-09T15:00:00",
    "model_type": "lightgbm",
    "predictions": [
      {
        "symbol": "600519.SH",
        "sector": "",
        "horizons": {
          "short_term": {"direction": "看涨", "probability": 0.62, "model": "lightgbm_short_term_5d"},
          "mid_term":   {"direction": "看涨", "probability": 0.71, "model": "lightgbm_mid_term_10d"},
          "long_term":  {"direction": "看跌", "probability": 0.55, "model": "lightgbm_long_term_20d"}
        }
      }
    ],
    "meta": {"models_loaded": 3, "error_symbols": []}
  }
  ```
- 行为：逐标的调用 `predict_all_horizons`；单标的失败计入 `meta.error_symbols`，**不整体 500**。
- 模型未加载：`meta.models_loaded == 0` → 28 客户端据此判定"服务不可用，跳过"（与现步骤 2.5 行为一致）。
- 实现位置：`src/api/server.py` 新增路由；复用 `_engine.predict_all_horizons` 与现有模型键命名 `f"{horizon_name}_{days}d"`。

### 4.2 28 客户端 `trendcast_client.py`（NEW）
- 类 `TrendCastClient(base_url="http://127.0.0.1:8800", timeout=10, retries=2)`。
- 方法：
  - `health_check() -> dict`：GET `/health`；异常/超时 → `{"error": "..."}`（fail-open）。
  - `get_portfolio_summary(symbols: list[str] | None = None) -> dict`：
    - 首选 GET `/api/v1/portfolio/summary?symbols=...`；
    - 若 16_ 版本不含该端点（4xx）→ 回退组合调用 `/api/v1/predict/batch` + `/api/v1/config/markets` 自行聚合（兼容旧/其它部署）；
    - 返回固定契约：`{"generated_at", "model_type", "predictions":[{symbol,sector,horizons:{...}}], "meta":{...}}`；异常 → `{"error": "..."}`。
  - `load_position_symbols() -> list[str]`：读 `config/positions.json`（`data["positions"]` 兼容 dict/list），提取 `code` 字段，返回去重标的列表（fail-safe，缺失返回 `[]`）。
- 所有网络调用包裹 try/except → 返回 `{"error": ...}` 而非抛异常；28 主流程零中断。

### 4.3 28 审计 `trendcast_audit.py`（NEW）
- 类 `TrendCastAudit(audit_dir="logs/trendcast/audit")`。
- 存储：JSONL（每行一条预测记录），字段：`symbol, horizon, direction, probability, source, predicted_at, verify_date, verified, hit`。
- 方法：
  - `record_prediction(symbol, horizon, direction, probability, source="trendcast_pro")`：追加记录，`verified=False`。
  - `verify_predictions(price_source=None, asof=None)`：对 `verified=False` 且到期（预测日 + horizon_days ≤ asof）的记录，调用 `price_source(symbol, predicted_at, horizon_days)` 取得真实未来收益方向；`hit = (direction=="看涨" and ret>0) or (direction=="看跌" and ret<0)`；写回 `verified=True, hit`。
  - `get_stats()`：`{total_predictions, verified, hits, hit_rate}`。
- **`price_source` 默认实现 = best-effort**：
  - 依次尝试：① 28 本地 `data/cache/**/<symbol>*.parquet`（pandas 读，定位 close 列与日期列，取预测日后第 `horizon_days` 交易日收益）；② 失败则 `return None`。
  - `None` → 记录保持 `verified=False`（不误判命中）。**明确不依赖 16_ 自身 simulation 审计**。
  - 生产环境可在构造时注入 28 规范行情源（如 `DataProvider` 扩展方法）→ 二期接真实命中率。
- 漂移告警：命中率 < 50% 且 `verified > 10` → 调用方输出 ⚠ 漂移告警（沿用步骤 2.5 语义）。

---

## 5. 决策注入位置（一期，只读）

| 注入点 | 方式 | 侵入性 |
|---|---|---|
| 每日报告 | `step_trendcast_predict()` 返回信号卡片文本，本就进入报告章节 | 零侵入 |
| LLM 决策上下文 | 写 `logs/trendcast/signals_YYYY-MM-DD.json` 快照；下游（ai_decision / v8.3 决策 prompt）可读取作为上下文 | 旁路，不改决策字段 |
| 调仓打分 | **一期不实现**（二期过命中率门禁后作为方向/置信度因子） | — |

> 注意 28 铁律：`v8.3_institutional/llm_intraday_decision_engine.py` 渲染只认 `DecisionResult` 真字段（`trading_signals`/`raw_analysis`）。一期**不向该引擎注入幽灵字段**；仅通过快照文件提供上下文，由决策模块自行选择是否消费。

---

## 6. 一期 / 二期范围

### 一期（本次实现）
- 16_：`/api/v1/portfolio/summary` 端点 + 单测。
- 28：`trendcast_client.py` + `trendcast_audit.py` + `daily_runner` 步骤 2.5 接线 + 单测；信号卡片进入每日报告 + 快照文件。
- 验证：链路跑通、fail-open 降级、跨仓各自提交。
- 预测数据接受 simulation 兜底（精度非验收项）。

### 二期（路线，本次不实现，需在文档固化）
1. 用 28 真实持仓池与行情数据扩标的 / 重训 16_（`config.data.source` 由 `simulation` 切换为 `tencent`/`wind`，补齐 `data/raw/<symbol>.csv`）。
2. `price_source` 接 28 规范行情源，使命中率判据真实可用。
3. 命中率与 IC 达标后，作为调仓打分因子注入（须先过 §5 门禁，决策路径 fail-close）。
4. ETF / 对冲标的预测接入。

---

## 7. 安全边界与防回归

- 观测路径 fail-open：16_ 不可用 / 超时 / 异常 → 客户端返回 `{"error":...}`，步骤 2.5 返回"TrendCast 不可用，跳过"，28 主流程不受影响。
- 决策路径 fail-close：一期任何改动**不得触碰** 28 下单/调仓路径；信号仅作上下文。
- 数据口径：16_ 自身审计基于 simulation，28 命中率判据必须用 28 真实行情（代码与注释显式说明）。
- 28 仓坑位：`config/*` gitignored（勿把运行配置当提交物）；根 commit 前 pre-commit P0 自检（`utils/` 改动触发 mypy 基线）；中文项目 ruff 规则（target py38、ignore RUF001-3）。
- 两仓独立 git：16_ 改动提交进 monorepo 根；28 改动提交进 28 仓。
- 新增文件完成即 `git add`（吸取 16_ 未跟踪文件丢失教训）。
- Windows 会话限制：长任务前台单命令执行（后台进程 60–90s 被清）。

---

## 8. 验证标准

- **契约测试**：16_ `test_api_summary.py`：响应含 `predictions/meta`；单标的失败入 `error_symbols` 不整体 500；模型未加载时 `meta.models_loaded==0`。
- **客户端单测**：`test_trendcast_client.py`（mock HTTP）：契约归一化正确；`/health` 超时 → `{"error"}`；服务不可达 → fail-open。
- **审计单测**：`test_trendcast_audit.py`（临时目录 + 注入 fake `price_source`）：落盘/验证/命中率/漂移告警正确。
- **端到端冒烟**：启动 16_ server（simulation 兜底），运行 28 `daily_runner` 步骤 2.5 → 真实产生信号卡片文本、审计 JSONL、命中率可统计；强制关掉 16_ server 复跑 → 步骤降级为"跳过"且 28 不报错。
- 提交：两仓分别 `git add` + commit，提交信息标注 [trendcast-integration]。

### 验证记录（2026-09-09 21:51，全部通过 ✅）

| 验证项 | 结果 |
|--------|------|
| 16_ 真实模型在线 | `/health` → `models_loaded: 3`（腾讯真实数据重训模型） |
| 正路径（server 在线） | 28 持仓 26 标的 × 3 周期 = 78 条预测；概率 43%~55% 各不相同（脱离 0.5 兜底）；信号卡片 + 快照 JSON + 审计 JSONL 全部落盘 |
| fail-open（server 关闭） | 3 次重试（WinError 10061）→ "TrendCast API 不可用，跳过预测"，28 主流程零中断 |
| 数据卫生 | simulation 时代 78 条旧审计记录已清除，仅保留真实模型记录（未来命中率统计无噪声） |
| 契约单测 | 28 侧 `test_trendcast_client.py` + `test_trendcast_audit.py` 10 passed |
| 提交 | 16_ 侧 `84c3aa3`（一期端点）+ `4fdf3fa`（真实数据链路重训）；28 侧 `9d953128`（client/audit/步骤2.5 接线） |

---

## 9. 落地文件清单

### 16_（提交进 monorepo 根 `e:\各种PY程序`）
- `16_金融市场预测模型/src/api/server.py` — [MODIFY] 新增 `/api/v1/portfolio/summary`。
- `16_金融市场预测模型/tests/test_api_summary.py` — [NEW] 端点契约单测。
- `16_金融市场预测模型/为28终极量化交易系统提供策略决策依据_设计方案_20260909.md` — [NEW] 本文档。

### 28（提交进 28 仓）
- `trendcast_client.py` — [NEW] 客户端（health / portfolio summary / 持仓读取 / fail-open）。
- `trendcast_audit.py` — [NEW] 审计（JSONL + 可插拔 price_source + 命中率/漂移）。
- `daily_runner.py` — [MODIFY] 步骤 2.5 适配新客户端契约，保持 fail-open 与 `--no-trendcast`。
- `tests/test_trendcast_client.py` — [NEW] mock 单测。
- `tests/test_trendcast_audit.py` — [NEW] 临时目录单测。
- 运行时产物（不入 git）：`logs/trendcast/audit/*.jsonl`、`logs/trendcast/signals_YYYY-MM-DD.json`。
