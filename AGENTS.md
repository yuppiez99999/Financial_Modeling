# 16_金融市场预测模型 协作规则

> 本项目使用 Project Cairn（路标石堆）组织项目知识：`AGENTS.md` 是规则与导航入口，`cairn/` 是项目知识/状态层。
> 同目录的 `CLAUDE.md` 只含一行 `@AGENTS.md`，让 Claude Code 读到同一套规则；Codex 直接读本文件。

## 项目一句话

A 股多周期预测与门禁风控系统（TrendCast Pro）：三周期概率模型 + 置信度门槛门禁 + ATR 风控建议，排期驱动（schedule/plan.json）的 AI 协作开发。

> 本文件由 `cairn init` 生成，已填入本项目自己的定位与 provider 配置；其他项目复用前应先跑各自的 init。

## 初始化配置

- 毕业 provider：暂缓对接（none yet — 首次毕业时再对接知识库）
- 知识库索引：暂未配置
- 毕业目标：暂未配置

## 进入项目后的阅读顺序

1. 先读本文件（AGENTS.md）。
2. 若存在 `cairn/ROADMAP.md`，读它了解路线图、当前焦点与开放问题（ROADMAP 可选；最小化初始化的项目可能没有）。
3. 读 `cairn/LOG.md` 最近几条（最新在最上）了解近期进展与关键决策。
4. 按当前任务需要读相关的 `cairn/` 知识专题文档。

## 文件职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md`（根目录） | 规则与导航 | 很少改动，≤ 60 行 |
| `CLAUDE.md`（根目录） | 一行 `@AGENTS.md` 存根 | 写一次，不再动 |
| `cairn/ROADMAP.md` | 路线图与进度 | 原地更新，保持精炼 |
| `cairn/LOG.md` | 时序日志 | 新条目加在最上方（最新在前），每条 ≤ 20 行，只写摘要 + 指针 |
| `cairn/<topic>.md` | 知识专题文档（当前真相） | 原地更新；坑与解法进正文小节，用 `contains` 标注；修订在 LOG 留指针 |
| `cairn/Reference/` | 外部原始输入 | 按需创建；只追加 |
| `cairn/Cited.md` | 知识库引用清单 | 只放指针，绝不复制原文 |

> 其他文件只在具体信号出现时才创建（要记一个决策、解掉一个坑、一个目标延续到会话之后）——不预建空壳。工程资产（被代码或流程消费的契约/配置/规格）不归本系统管，留在代码树里，不进 `cairn/`。

## 冲突仲裁规则

- 优先级：**知识专题文档 > LOG 历史**；规则级冲突以本文件为准。
- 业务/设计结论以 `cairn/` 知识专题文档里的最新记录为准，不认更旧的 LOG 条目。

## 知识库消费反射

- 在有够格毕业的可复用内核的工作开始前——即其产出或依赖的结论够格毕业时——先查阅本项目自己的 cairn/ 知识专题文档；外部知识库尚未对接（provider 暂缓对接——见上方初始化配置），外部索引检查与 cairn/Cited.md 引用在对接之后才启用。

## 文档协作规则

- 改动前先判断用户想要「讨论/建议」还是「直接改文档」；当用户说「先看看/先评估」时，先给分析——不要直接重写正式文档。
- 修正过去的判断时追加更正说明，不静默覆盖。
- 不把未经确认的判断写成定论。

## 知识沉淀规则

- 每个实质性进展之后，在 `cairn/LOG.md` 顶部加一条（摘要 + 指针）；让结论沉淀进 `cairn/` 知识专题文档。
- 跨项目可复用的经验通过毕业机制沉淀，待知识库对接后启用（provider 暂缓对接——见上方初始化配置）。
