# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
*.whl
dist/
build/

# Virtual environments
.venv/
env/
venv/

# IDE / editors
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# OS / temp
*.tmp
*.log

# 本项目日志目录
schedule/logs/*.json

# 本地运行留下的临时产物
*.bak

__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# 虚拟环境
.venv/
venv/

# 运行时产物目录
logs/
# Issue #55 离线复现：38 池日K（腾讯源 A股/ETF，2026-09-17 采集）与各命令读数产出，
# 作为**冻结快照**入库（date,open,high,low,close,volume）。冻结就是为了不再依赖采集
# 时刻：网络受限时缺行的短板已实测 —— edge-check 内连接对齐，缺行会把复现读数带偏
# 而**不会**失败。路径名以 `.frozen.` 段显式区别于运行产物目录 reports/ 与 data/raw/。
reports/**
!reports/**/*.frozen.*.json
data/raw/**
!data/raw/**/*.frozen.*.csv
# S13/G3：qlib .bin 列存与导出因子（离线生成物，不入 git）
data/qlib_bin/
data/qlib_factors/
data/processed/
data/news/
# 盘中实时快照（Q3）：分钟级数据绝不入 git，也绝不混入 data/raw 日K缓存
data/realtime/
models/exported/

# 训练产物模型（保留占位模型由 scripts 生成，不提交训练产物）
models/**/*.pkl
# S19 概率校准参数：由本机数据拟合，属运行产物（main.py calibration 重新生成）
models/probability_calibration_*.json
models/**/*.onnx

# Jupyter
.ipynb_checkpoints/
*.ipynb

# 系统
.DS_Store

# 量化交易适配层输出
output/

# 临时提交消息与草稿文件
__commit_msg.txt
__tmp_*.*
_*_msg.txt
_*_tmp.txt

# 临时调试脚本（schedule 目录下以下划线开头的辅助脚本）
schedule/_*.py

# 数据缓存目录
data/cache/


# TradingView 交付产物（tv-export）：与 reports/ 同性质，运行产物不入库
outputs/
